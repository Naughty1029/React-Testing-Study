{
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1"
  },
  "scripts": {
    "prepare": "husky install",
    "fix": "npm run format && npm run lint:fix",
    "format": "prettier --write 'rtl_study/src/**/*.{js,jsx,ts,tsx}'",
    "lint:fix": "eslint --fix 'rtl_study/src/**/*.{js,jsx,ts,tsx}'"
  }
}
